package io.polygon.android.build

object Dependencies {

    // NOTE: Be sure to keep this value up to date with the top level gradle file
    //       since it can't be referenced there
    const val KotlinVersion = "1.3.50"

    const val koinVersion = "2.1.5"

    const val threeTenAbpVersion = "1.2.3"
}
